spring.application.name=back-end

# Configuração de conexão com o banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/g1?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=pedrodev123#

# Driver do banco de dados (no caso do MySQL)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate (configuração automática do Spring Boot para usar JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
